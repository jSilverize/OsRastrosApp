{
  "rules": {
    "users": {
      "$user_id": {
    		".read": true,
        ".write": "$user_id === auth.uid"
      }
    },
    "going" : {
    	".read": true,
      ".write": "auth !== null"
    }
  }
}
